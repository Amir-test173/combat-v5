# World Dominion 1.2 — تقرير الحفاظ على v1.1

تاريخ المراجعة: 24 أغسطس 2026

هذا التقرير موجود خصيصًا لمنع فقدان الميزات أثناء التطوير التراكمي.

## خط الأساس

تم إنشاء 1.2 مباشرة من مشروع `world_dominion_flutter_v1.1_gameplay` الذي تم التحقق من مطابقته للنسخة المرجعية داخل Continuation Pack.

## نتيجة مقارنة الملفات

- عدد ملفات v1.1 المرجعية قبل التطوير: **43 ملفًا**.
- لم يُحذف أي ملف من ملفات v1.1: **0 ملفات محذوفة**.
- الملفات الوظيفية الجديدة في 1.2 قبل إضافة هذا التقرير:
  - `GAMEPLAY_V12_AR.md`
  - `server/combat_rules.js`
  - `server/combat_rules_test.js`
- هذا الملف `PRESERVATION_V12_AR.md` هو تقرير إضافي فقط.
- بقية التغييرات تمت داخل ملفات موجودة أصلًا لتوسيع النظام، لا لاستبدال المشروع أو الرجوع إلى نسخة أقدم.

## أمثلة على أن الأنظمة القديمة بقيت موجودة

- `reinforceFieldArmy` / `reinforceArmy`: تعزيز وحدات الجيش الميداني القديم بقي موجودًا.
- `reinforceBattle`: أمر جديد مختلف لإدخال جيش احتياط كامل إلى جبهة نشطة.
- نقاط القيادة وخطط الهجوم والبحث وإرهاق الحرب والأحداث وسياسات الاحتلال بقيت موجودة.
- الغزو البرمائي والممرات البحرية والإمداد البري/البحري بقيت موجودة.
- المنشآت الاستراتيجية والغارات والحصار والتجارة والعقوبات والتجسس والتحالفات بقيت موجودة.
- الحفظ المحلي وSnapshot وPostgreSQL وResume Token وLobby والـModeration بقيت موجودة.
- اختبار المسار المرجعي `USA → GBR` بقي ضمن اختبارات Flutter.

## التوافق

- إصدار التطبيق: `1.2.0-combat`.
- بروتوكول الأونلاين: **Protocol 3**.
- حقل `battles` جديد واختياري عند قراءة Snapshot؛ الحفظ القديم من 1.1 الذي لا يحتوي عليه يحمّل بقية الحالة ثم يبدأ بدون جبهات نشطة.
- يجب استخدام تطبيق 1.2 مع سيرفر 1.2 في الأونلاين.

## فحوص أُجريت في هذه البيئة

- `node --check` للسيرفر: ناجح.
- `npm run check`: **6/6 اختبارات ناجحة**.
- مقارنة السيرفر المنفصل مع `server/` داخل مشروع Flutter: يجب أن تبقى متطابقة قبل التغليف النهائي.
- فحص توازن الأقواس في `lib/main.dart`: متوازن.
- لا يتوفر Flutter/Dart SDK في بيئة التنفيذ الحالية، لذلك لم يتم الادعاء بأن `flutter analyze` أو `flutter test` أو `flutter build apk` نُفذت محليًا. GitHub Actions مهيأ لتنفيذها في بيئة Flutter حقيقية.

## قاعدة التطوير التالية

عند تطوير 1.3 وما بعده، تكون هذه الحزمة هي خط الأساس الجديد. أي ميزة موجودة يجب توسيعها أو ربطها بالنظام الجديد بدل إعادة إضافتها باسم آخر، وأي تغيير في الحفظ أو البروتوكول يجب أن يراعي التوافق أو يضيف Migration واضحًا.
